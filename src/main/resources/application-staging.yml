# Profil STAGING (VPS Dokploy)
spring:
  config:
    activate:
      on-profile: staging
  jpa:
    show-sql: true

logging:
  level:
    com.modeltech: DEBUG
    org.springframework.security: DEBUG

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  masterclass:
    meet-link: ${MASTERCLASS_MEET_LINK:https://meet.google.com/xxx-xxxx-xxx}
